// Copy this file to the build directory and try to include it. Observe that it
// is only possible if you do this: set(CMAKE_INCLUDE_CURRENT_DIR true).
// It doesn't matter where that CMake variable is set, be it in the
// CMakeLists.txt file or some CMake module.

#include <iostream>

std::string message = "Bo ya!";